# ============================================================================
# chery-ADAS-Server 部署配置 —— 换机器 / 换用户时,只改这一个文件
# docker compose 会自动读取同目录下的 .env。改完需重新 build 才生效:
#   docker compose build
# ============================================================================

# ── 容器内用户(关键:UID/GID 必须与宿主机当前用户一致,否则挂载卷 / git 权限会冲突)──
# 查询宿主机用户的 UID/GID:  id <你的用户名>
# 例:weiyu → uid=1005, gid=1006
USERNAME=carla
USER_UID=1005
USER_GID=1006

# ── LEAD 源码在宿主机的路径(读写挂载到容器 /workspace/lead)──
# 改代码即时反映到容器,git 在宿主机管理。
LEAD_SRC=/home/weiyu/workspace/lead

# ── CARLA 启动参数(由 start_carla.sh / compose 读取)──
# 质量:Epic(默认,正式打分用)/ Poor(调试省显存,会引入 distribution shift)
CARLA_QUALITY=Epic
CARLA_RPC_PORT=2000

# 说明:
# - 宿主机 weiyu 的 id 为 uid=1005(weiyu) gid=1006(weiyu)。本文件默认值即据此设定。
# - 若换到 UID/GID 不同的机器,改上面 USER_UID / USER_GID 为 `id` 查到的值即可,
#   两个容器(carla-server / lead-eval)会一起套用,无需逐个改 Dockerfile。
# - CARLA 无需配置:carla-server 镜像构建时自动下载 0.9.15;lead-eval 的 PythonAPI 由 entrypoint 下载。
